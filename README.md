# Data Sync Utility

Internal utility for synchronizing distributed data shards using automated browsers.

## Usage
Scheduled execution via CI/CD pipeline.
Requires environment configuration for backend connectivity.